@{
    ViewBag.Title = "Acceso denegado";
}

<div class="card shadow-sm p-4 mt-4 text-center border-danger">
    <h2 class="text-danger mb-3">
        <i class="fas fa-ban me-2"></i> Acceso denegado
    </h2>
    <p class="lead">🚫 No tienes permisos para acceder a esta página.</p>
    <a href="/Administrador/PanelAdmin" class="btn btn-primary mt-3 btn-back">
        <i class="fas fa-home me-2"></i> Volver al inicio
    </a>
</div>

<!-- FontAwesome -->
<link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">

<!-- CSS personalizado -->
<style>
    h2 {
        font-weight: 700;
        border-bottom: 3px solid #dc3545;
        display: inline-block;
        padding-bottom: 0.3rem;
    }

    .card {
        border-radius: 12px;
        background: #fff;
    }

    .btn-back {
        background: linear-gradient(135deg, #007bff, #0dcaf0);
        border: none;
        font-weight: 600;
        border-radius: 30px;
        transition: all 0.3s ease;
        padding: 0.6rem 1.2rem;
    }

        .btn-back:hover {
            background: linear-gradient(135deg, #0dcaf0, #007bff);
            transform: translateY(-2px);
        }
</style>